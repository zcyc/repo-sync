[package]
name = "repo-sync"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.2.20", features = ["derive"] }
serde = { version = "1.0.144", features = ["derive"] }
serde_json = "1.0.85"
job_scheduler_ng = "*"

[profile.release]
lto = true
strip = true
