[package]
name = "repo-sync"
version = "1.0.0"
edition = "2021"

[dependencies]
clap = { version = "4.0.14", features = ["derive"] }
serde = { version = "1.0.145", features = ["derive"] }
toml = "1.1.0"
job_scheduler_ng = "2.0.2"

[profile.release]
lto = true
strip = true
